document.getElementById("criseForm").addEventListener("submit", function(event) {
    event.preventDefault();
    alert("Merci pour vos réponses ! Les résultats ont été enregistrés.");
    // Tu peux ajouter ici du code pour envoyer les réponses à un serveur ou les afficher.
});
